import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.payetonkawa',
  appName: 'payetonkawa',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
