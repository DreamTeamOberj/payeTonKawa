import {
    IonButton,
    IonContent,
    IonGrid,
    IonRow,
    IonHeader,
    IonInput,
    IonItem,
    IonLabel,
    IonList,
    IonPage,
    IonTitle,
    IonToolbar
} from '@ionic/react';

import React from "react";

const Profile: React.FC = () => {
    return (
        <IonPage>
            <IonContent className="ion-padding">
            </IonContent>
        </IonPage>
    );
};

export default Profile;
